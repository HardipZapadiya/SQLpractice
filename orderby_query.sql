use MyDatabase;


--ordering in descending order 
select *
from customers 
order by score desc;

--order in Ascending order 
--by default record will be order in ascending order 


select * 
from customers 
order by score asc;